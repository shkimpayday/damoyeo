# 모임 수정 기능 구현 가이드

## 개요

모임장(OWNER) 또는 운영진(MANAGER)이 모임 상세 페이지에서 "모임수정" 버튼을 클릭하면 모임 정보를 수정할 수 있는 기능을 구현합니다. 배너 이미지(coverImage) 수정 기능도 포함됩니다.

---

## 현재 상태 분석

### 이미 구현된 것
- ✅ 백엔드 API: `PUT /api/groups/{id}` (FormData 지원)
- ✅ 프론트엔드 API 함수: `updateGroup()` in `groups-api.ts`
- ✅ TanStack Query Hook: `useUpdateGroup()` in `use-groups.ts`
- ✅ 라우트: `/groups/:groupId/edit` (Protected Route)
- ✅ 수정 페이지: `edit-page.tsx` (기본 구현 완료)

### 수정이 필요한 것
- ❌ `detail-page.tsx`: "모임수정" 버튼이 `handleJoin` 호출 (버그)
- ❌ `edit-page.tsx`: 배너 이미지 수정 기능 없음

---

## 구현 단계

### 1단계: 상세 페이지 버튼 수정

**파일**: `src/app/routes/pages/groups/detail-page.tsx`

**현재 코드 (304-310줄)** - 버그:
```tsx
<button
  onClick={handleJoin}  // ❌ 잘못된 핸들러
  disabled={joinMutation.isPending}
  className="w-full py-3.5 bg-primary-500 ..."
>
  모임수정
</button>
```

**수정 방법**:

1. 상단에 `Link` import 확인 (이미 있다면 생략):
```tsx
import { useParams, useNavigate, Link } from "react-router";
```

2. 버튼을 Link 컴포넌트로 변경:
```tsx
<Link
  to={`/groups/${group.id}/edit`}
  className="w-full py-3.5 bg-primary-500 text-white rounded-xl font-semibold hover:bg-primary-600 transition-colors text-center block"
>
  모임 수정
</Link>
```

**전체 하단 버튼 영역 구조 (권장)**:
```tsx
{/* Bottom Action Button */}
<div className="fixed bottom-16 left-0 right-0 p-4 bg-white/95 backdrop-blur-sm border-t border-gray-100">
  <div className="max-w-[1024px] mx-auto">
    {group.myRole ? (
      <div className="flex gap-3">
        {/* OWNER가 아닌 멤버: 탈퇴 버튼 */}
        {group.myRole !== "OWNER" && (
          <button
            onClick={handleLeave}
            className="flex-1 py-3.5 text-center border-2 border-gray-200 text-gray-600 rounded-xl font-semibold hover:bg-gray-50 transition-colors"
          >
            탈퇴하기
          </button>
        )}

        {/* OWNER 또는 MANAGER: 수정 버튼 */}
        {(group.myRole === "OWNER" || group.myRole === "MANAGER") && (
          <Link
            to={`/groups/${group.id}/edit`}
            className="flex-1 py-3.5 bg-primary-500 text-white rounded-xl font-semibold hover:bg-primary-600 transition-colors text-center"
          >
            모임 수정
          </Link>
        )}
      </div>
    ) : (
      /* 비멤버: 가입 버튼 */
      <button
        onClick={handleJoin}
        disabled={joinMutation.isPending}
        className="w-full py-3.5 bg-primary-500 text-white rounded-xl font-semibold hover:bg-primary-600 transition-colors disabled:opacity-50 shadow-lg shadow-primary-500/25"
      >
        {joinMutation.isPending ? "가입 중..." : "가입하기"}
      </button>
    )}
  </div>
</div>
```

---

### 2단계: 수정 페이지에 배너 이미지 기능 추가

**파일**: `src/app/routes/pages/groups/edit-page.tsx`

#### 2-1. 상태 추가

기존 상태들 아래에 이미지 관련 상태 추가:
```tsx
// 기존 상태들
const [name, setName] = useState("");
const [description, setDescription] = useState("");
// ... 기타 상태

// 이미지 관련 상태 추가
const [coverImage, setCoverImage] = useState<File | null>(null);
const [imagePreview, setImagePreview] = useState<string | null>(null);
```

#### 2-2. useEffect에서 기존 이미지 미리보기 설정

```tsx
useEffect(() => {
  if (group) {
    setName(group.name);
    setDescription(group.description || "");
    setCategoryId(group.category.id);
    setAddress(group.address || "");
    setMaxMembers(group.maxMembers);
    setIsPublic(group.isPublic);

    // 기존 커버 이미지가 있으면 미리보기 설정
    if (group.coverImage) {
      setImagePreview(group.coverImage);
    }
  }
}, [group]);
```

#### 2-3. 이미지 변경 핸들러 추가

```tsx
const handleImageChange = (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0];
  if (file) {
    // 파일 크기 검증 (5MB 제한)
    if (file.size > 5 * 1024 * 1024) {
      alert("이미지 크기는 5MB 이하여야 합니다.");
      return;
    }

    // 파일 타입 검증
    if (!file.type.startsWith("image/")) {
      alert("이미지 파일만 업로드 가능합니다.");
      return;
    }

    setCoverImage(file);

    // 미리보기 생성
    const reader = new FileReader();
    reader.onloadend = () => {
      setImagePreview(reader.result as string);
    };
    reader.readAsDataURL(file);
  }
};

const handleImageRemove = () => {
  setCoverImage(null);
  setImagePreview(null);
};
```

#### 2-4. 폼 제출 핸들러 수정

```tsx
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();

  if (!name.trim()) {
    alert("모임 이름을 입력해주세요.");
    return;
  }

  try {
    await updateMutation.mutateAsync({
      groupId: Number(groupId),
      request: {
        name: name.trim(),
        description: description.trim(),
        categoryId,
        address: address.trim(),
        maxMembers,
        isPublic,
        coverImage: coverImage || undefined,  // 이미지 추가
      },
    });
    navigate(`/groups/${groupId}`);
  } catch (error) {
    console.error("모임 수정 실패:", error);
    alert("모임 수정에 실패했습니다.");
  }
};
```

#### 2-5. 이미지 업로드 UI 추가

폼 내부에 이미지 업로드 섹션 추가 (모임 이름 필드 위 또는 아래):

```tsx
{/* 배너 이미지 */}
<div>
  <label className="block text-sm font-medium text-gray-700 mb-2">
    배너 이미지
  </label>

  {/* 이미지 미리보기 */}
  {imagePreview ? (
    <div className="relative mb-3">
      <img
        src={imagePreview}
        alt="배너 미리보기"
        className="w-full h-48 object-cover rounded-xl"
      />
      <button
        type="button"
        onClick={handleImageRemove}
        className="absolute top-2 right-2 w-8 h-8 bg-black/50 text-white rounded-full flex items-center justify-center hover:bg-black/70 transition-colors"
      >
        ✕
      </button>
    </div>
  ) : (
    <div className="w-full h-48 bg-gray-100 rounded-xl flex items-center justify-center mb-3">
      <span className="text-gray-400">이미지 없음</span>
    </div>
  )}

  {/* 파일 선택 버튼 */}
  <label className="block">
    <input
      type="file"
      accept="image/*"
      onChange={handleImageChange}
      className="hidden"
    />
    <div className="w-full py-3 border-2 border-dashed border-gray-300 rounded-xl text-center text-gray-500 cursor-pointer hover:border-primary-400 hover:text-primary-500 transition-colors">
      {imagePreview ? "이미지 변경" : "이미지 업로드"}
    </div>
  </label>

  <p className="mt-1 text-xs text-gray-500">
    권장 크기: 1200x400px, 최대 5MB
  </p>
</div>
```

---

### 3단계: 타입 확인

**파일**: `src/features/groups/types/index.ts`

`GroupUpdateRequest` 타입에 `coverImage` 필드가 있는지 확인:
```tsx
export interface GroupUpdateRequest {
  name?: string;
  description?: string;
  categoryId?: number;
  coverImage?: File;      // ✅ 이미 있음
  address?: string;
  lat?: number;
  lng?: number;
  maxMembers?: number;
  isPublic?: boolean;
}
```

---

## 전체 수정 파일 목록

| 파일 | 수정 내용 |
|------|----------|
| `detail-page.tsx` | "모임수정" 버튼을 Link로 변경, 권한별 버튼 분기 |
| `edit-page.tsx` | 이미지 상태/핸들러 추가, 이미지 업로드 UI 추가 |

---

## 테스트 체크리스트

### 상세 페이지 (detail-page.tsx)
- [ ] 모임장(OWNER)으로 접속 시 "모임 수정" 버튼 표시
- [ ] 운영진(MANAGER)으로 접속 시 "모임 수정" 버튼 표시
- [ ] 일반 멤버로 접속 시 "탈퇴하기" 버튼만 표시
- [ ] 비멤버로 접속 시 "가입하기" 버튼 표시
- [ ] "모임 수정" 클릭 시 `/groups/{id}/edit` 페이지로 이동

### 수정 페이지 (edit-page.tsx)
- [ ] 기존 모임 정보가 폼에 자동 입력됨
- [ ] 기존 배너 이미지가 미리보기에 표시됨
- [ ] 새 이미지 선택 시 미리보기 업데이트
- [ ] 이미지 제거 버튼 동작
- [ ] 5MB 초과 이미지 업로드 시 경고
- [ ] 수정 완료 후 상세 페이지로 이동
- [ ] 권한 없는 사용자 접근 시 에러 표시

---

## 참고: 관련 파일 경로

```
프론트엔드:
├── src/app/routes/pages/groups/
│   ├── detail-page.tsx    ← 버튼 수정
│   └── edit-page.tsx      ← 이미지 기능 추가
├── src/features/groups/
│   ├── api/groups-api.ts  ← updateGroup() 함수 (수정 불필요)
│   ├── hooks/use-groups.ts ← useUpdateGroup() hook (수정 불필요)
│   └── types/index.ts     ← GroupUpdateRequest 타입 (확인만)

백엔드 (수정 불필요, 참고용):
├── GroupController.java   ← PUT /api/groups/{id}
├── GroupModifyRequest.java ← 요청 DTO
└── GroupServiceImpl.java  ← modify() 메서드
```

---

## 추가 개선 사항 (선택)

1. **이미지 압축**: 업로드 전 클라이언트에서 이미지 리사이징
2. **드래그 앤 드롭**: 이미지 드래그 앤 드롭 업로드 지원
3. **위치 정보 수정**: Geolocation API 연동으로 위치 재설정
4. **수정 확인 모달**: 저장 전 변경사항 확인 다이얼로그
